services:
  nginx:
    image: nginx:alpine
    container_name: portfolio
    ports:
      - "7070:80"
    volumes:
      - /home/casabb/Projects/portfolioV1/dev/dist:/usr/share/nginx/html:ro
    restart: unless-stopped